// Arquivo de teste gerado para components/Cardforlulario.tsx
import { describe, it, expect } from "vitest";

describe('Cardforlulario', () => {
  it('deve funcionar corretamente', () => {
    expect(true).toBe(true);
  });
});
