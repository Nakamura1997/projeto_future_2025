// Arquivo de teste gerado para components/Header.tsx
import { describe, it, expect } from "vitest";

describe('Header', () => {
  it('deve funcionar corretamente', () => {
    expect(true).toBe(true);
  });
});
